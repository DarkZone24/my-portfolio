# DO NOT commit real secrets. Replace placeholders with your real keys locally.
# Rotate/revoke the exposed key in the OpenAI dashboard (https://platform.openai.com/account/api-keys).
OPENAI_API_KEY=REPLACE_WITH_OPENAI_KEY

# When true the server returns a canned development reply and skips real API calls.
DEV_MOCK_OPENAI=true

# To use Gemini (Vertex AI) set USE_GEMINI=true and configure the variables below.
USE_GEMINI=false
GOOGLE_PROJECT_ID=your-gcloud-project-id
GOOGLE_LOCATION=us-central1
GEMINI_MODEL=chat-bison-001

# If you use a service-account JSON for Google ADC, set the path here (do NOT commit the JSON file):
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\service-account-key.json
